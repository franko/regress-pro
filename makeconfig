
PRODUCT_REGISTRATION = yes
ENABLE_DEBUG = yes

CC = gcc
CFLAGS = -g -Wall
CXXFLAGS = -g

ifeq ($(ENABLE_DEBUG), yes)
  DEFS = -DDEBUG_MEM -DDEBUG_REGRESS
endif

ifeq ($(PRODUCT_REGISTRATION), yes)
  DEFS += -DREGPRO_REGISTRATION
endif
