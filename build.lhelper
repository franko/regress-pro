CC="${CC:-gcc}"
CXX="${CXX:-g++}"

BUILD_TYPE=Release

packages=(
  "nlopt"
  "openblas -arch=static"
  "gsl -blas=openblas"
)

if [[ "$OSTYPE" == "msys"* || "$OSTYPE" == "mingw"* ]]; then
  packages+=("fox")
else
  packages+=("freetype2" "expat" "fontconfig" "xft" "fox -xft")
fi

